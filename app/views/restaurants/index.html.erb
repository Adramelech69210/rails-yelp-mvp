<div class="container mt-5">
  <h1>Restaurants</h1>

  <ul class="list-group">
    <% @restaurants.each do |restaurant| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <div>
          <%= restaurant.name %> - <%= restaurant.address %>
        </div>
        <div>
          <%= link_to "Voir détails", restaurant_path(restaurant), class: "btn btn-primary btn-sm" %>
        </div>
      </li>
    <% end %>
  </ul>

  <div class="mt-4">
    <%= link_to "Nouveau restaurant", new_restaurant_path, class: "btn btn-success" %>
  </div>
</div>
